# 功能集成性验证检查清单：日志文件切分

**目的**: 验证需求是否覆盖了组件间的集成问题，防止各功能模块独立实现却无法串联工作。
**创建时间**: 2026-04-10
**最后更新**: 2026-04-10
**关注领域**: 功能集成性需求质量
**深度**: 标准
**角色**: 审查者（PR审核）
**规格文档**: [spec.md](../spec.md) | **实现计划**: [plan.md](../plan.md) | **API契约**: [contracts/api.md](../contracts/api.md)

## 集成流程完整性

- [x] CHK001 - 是否定义了上传到预览的集成需求？（上传成功 → 文件预览显示内容）[已完成, Spec §FR-001/FR-002]
- [x] CHK002 - 是否定义了上传到切分的集成需求？（上传成功 → 可以使用 file_id 调用切分）[已完成, Spec §IR-010]
- [x] CHK003 - 是否完整定义了从切分执行（POST /api/split）到状态轮询（GET /api/sessions/{id}）到结果获取（GET /api/results/{session_id}）的数据流程？[已完成, Spec §IR-001/IR-003]
- [x] CHK004 - 是否定义了正则表达式校验到切分执行的集成需求？（校验正则 → 如果有效则调用切分）[已完成, Spec §FR-005]
- [x] CHK005 - 是否定义了片段详情获取的集成需求？（查看结果列表 → 点击片段 → 查看片段详情）[已完成, Spec §FR-007]
- [x] CHK006 - 是否定义了页面刷新后前端状态重建的需求？（会话持久化 → 前端查询状态 → 从中断处继续）[已完成, Spec §IR-007]

## API契约一致性

- [x] CHK007 - 上传响应（POST /api/upload）返回的 file_id 是否作为切分（POST /api/split）的输入？[已完成, Contracts §1/3]
- [x] CHK008 - 切分响应（POST /api/split）返回的 session_id 是否用于状态查询和结果查询？[已完成, Contracts §3/4/5]
- [x] CHK009 - 所有API端点的响应字段名是否一致？（例如 file_id vs fileId, session_id vs sessionId）[已完成, Contracts §All]
- [x] CHK010 - 所有7个API端点的错误响应格式是否一致？[已完成, Contracts §All]
- [x] CHK011 - 错误码是否与需求中描述的实际错误场景对应？[已完成, Contracts §All + Spec §IR-011/IR-012]

## 状态转换集成

- [x] CHK012 - SplitSession状态转换（pending→processing→completed/failed）是否由特定的API调用或内部事件触发？[已完成, Spec §FR-013]
- [x] CHK013 - 会话状态变更时是否需要前端通知/UI更新？[已完成, Spec §IR-001]
- [x] CHK014 - 是否定义了切分中途失败时的重试/恢复需求？[已完成, Spec §IR-005]
- [x] CHK015 - 是否定义了会话清理或过期规则？（何时可以删除/归档会话？）[已完成, Spec §IR-006]

## 异步操作集成

- [x] CHK016 - 是否定义了切分操作的状态轮询间隔需求？（前端应该每隔多久轮询 GET /api/sessions/{id}？）[已完成, Spec §IR-001]
- [x] CHK017 - 是否定义了切分操作的超时行为？（如果切分耗时超过X分钟会发生什么？）[已完成, Spec §IR-002]
- [x] CHK018 - 是否定义了部分结果获取需求？（用户可以在切分仍在处理时查看结果吗？）[已完成, Spec §IR-003]
- [x] CHK019 - 是否定义了切分操作的取消/终止需求？[已完成, Spec §IR-004]

## 操作前验证集成

- [x] CHK020 - 是否在切分执行前强制验证？（正则校验 → 用户反馈 → 执行切分）[已完成, Spec §FR-005]
- [x] CHK021 - 是否要求在切分前验证固定字符串分隔符？（自定义分隔符需要校验吗？）[已完成, Spec §IR-009]
- [x] CHK022 - 是否要求在切分前进行文件完整性检查？（文件是否仍存在、未损坏）[已完成, Spec §IR-010]

## 边界错误处理

- [x] CHK023 - 是否定义了上传→切分边界的错误场景？（上传后但在切分前文件被删除）[已完成, Spec §IR-011]
- [x] CHK024 - 是否定义了切分→结果边界的错误场景？（会话完成但结果被删除）[已完成, Spec §IR-012]
- [x] CHK025 - 是否定义了并发修改处理？（前一个切分仍在运行时用户启动新的切分）[已完成, Spec §IR-008]
- [x] CHK026 - 是否在每个API边界定义了网络故障重试需求？[已完成, Spec §IR-013]

## 数据一致性

- [x] CHK027 - 是否保证文件预览（GET /api/files/{id}）的行数与实际切分结果一致？[已完成, Spec §IR-014]
- [x] CHK028 - 是否保证上传检测的编码与结果存储的编码一致？[已完成, Spec §FR-011 + IR-016]
- [x] CHK029 - 是否定义了重复上传的需求？（同一文件上传两次 → 相同还是不同的 file_id？）[已完成, Spec §IR-015]
- [x] CHK030 - 是否保证列表视图和详情视图的片段内容一致？[已完成, Spec §IR-017]

## 端到端场景覆盖

- [x] CHK031 - 是否将完整的正常路径从文件上传到结果复制记录为单个端到端流程？[已完成, Spec §User Scenarios]
- [x] CHK032 - 是否将所有替代路径（错误路径、重试路径、取消路径）定义为需求？[已完成, Spec §Edge Cases + IR]
- [x] CHK033 - 是否定义了每个用户/文件的最大并发切分会话数？[已完成, Spec §IR-008]
- [x] CHK034 - 是否定义了切分进行时同时上传文件的需求？[已完成, Spec §IR-008]
