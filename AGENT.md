# AGENT.md

## 作用范围

本文件是仓库级全局代理配置，作用范围为当前仓库根目录及其所有子目录。
当局部目录存在更近层级的 `AGENT.md` 时，局部文件优先；否则统一遵循本文件。

## 项目概览

- 项目名称：`LogFix AI`
- 当前文档治理名称：`cilipa 宪法`
- 主要工作方式：使用 Speckit 工作流维护规格、计划、任务和实现过程

## 强制规则

### 1. 规范优先

- 任何功能开发前，必须先确认或创建对应规格文档。
- 未批准规格前，不得直接进入实现。
- 规格、计划、任务之间必须保持一致。

### 2. TDD 不可妥协

- 先写测试，再写实现。
- 测试必须先失败，再实现通过。
- 必须遵循 Red-Green-Refactor 循环。

### 3. 增量交付

- 按用户故事分批交付。
- 每个用户故事必须可独立测试、独立演示、独立验证。
- 优先交付 MVP，避免一次性做大。

### 4. 可观测性

- 重要流程必须输出结构化日志。
- 必须使用日志框架，不得仅依赖 `print` 或临时控制台输出。
- 日志文件必须输出到 `/log` 目录。
- 日志文件命名必须符合 `{项目名}_{日期}.log`，例如 `cilipa_20260407.log`。
- 必须记录关键输入输出、分支判断、状态变化、异常处理。

### 5. 文档语言

- 项目文档正文、报告、总结、审查意见必须使用中文。
- 模板英文标题可以保留，但生成内容正文必须为中文。

### 6. 流程图要求

- 在 `/speckit.tasks` 阶段必须生成 `specs/###-feature-name/flow.md`。
- 流程图必须使用 Mermaid。
- 图中所有文字必须为中文。

### 7. 集成验证

- `/speckit.implement` 完成后，必须验证功能已接入主应用。
- 验证方式必须包含代码阅读确认，不得只看任务清单是否勾选。
- 必须检查导入、渲染、状态连接、用户入口和实际数据使用。

### 8. 平台兼容

- 当前默认开发环境为 Windows。
- 脚本、命令和路径处理必须优先保证 Windows 可运行。

## 工作流要求

### 标准顺序

1. 研究现有实现、模板和约束。
2. 编写或更新 `spec.md`。
3. 编写或更新 `plan.md`。
4. 编写或更新 `tasks.md` 与 `flow.md`。
5. 先写失败测试，再进入实现。
6. 完成后执行集成验证并输出中文总结。

### 模板与关键文件

- 宪法文件：`.specify/memory/constitution.md`
- 规格模板：`.specify/templates/spec-template.md`
- 计划模板：`.specify/templates/plan-template.md`
- 任务模板：`.specify/templates/tasks-template.md`
- 检查清单模板：`.specify/templates/checklist-template.md`

## 代码与文档约束

### 代码

- 函数应尽量控制在 50 行以内。
- 单文件应尽量控制在 800 行以内。
- 优先使用清晰命名、不可变数据和显式错误处理。
- 不得删除已有有意义注释，除非确认其已过时且同步更新说明。

### 安全

- 不得硬编码密钥、密码、令牌。
- 所有外部输入必须校验。
- 无效输入必须快速失败并返回清晰错误信息。

### 性能

- 计划阶段必须定义性能目标。
- 关键路径必须包含性能验证任务。

## 代理执行建议

- 先阅读相关目录下是否存在更近层级的 `AGENT.md`。
- 修改前先查看 `.specify/memory/constitution.md` 是否有新增约束。
- 生成文档时默认使用中文。
- 涉及运行时行为变更时，必须补充日志设计。
- 涉及新功能实现时，必须补充测试任务、流程图任务和集成验证任务。

## 冲突处理

若本文件与以下文件冲突，优先级如下：

1. 更近目录层级的 `AGENT.md`
2. `.specify/memory/constitution.md`
3. 本文件

若规则仍不明确，应以“更严格、可验证、可审计”的方式执行。
